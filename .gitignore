node_modules/
out/
dist/
# `/` מוביל = שורש הפרויקט בלבד. בלעדיו הדפוס תופס כל תיקייה בשם הזה –
# `tools/release/` נבלעה כך ומעולם לא נכנסה ל-git, והשחרור נכשל.
/release/
*.db
*.db-shm
*.db-wal
# נתוני אמת – לעולם לא עולים לריפו הציבורי.
# חוברת האקסל המקורית מכילה 90 חברים אמיתיים עם טלפונים והיסטוריה כספית.
data/legacy/
data/backups/
data/import-report.md
data/import-cache/
.eslintcache
coverage/
*.local
.DS_Store
Thumbs.db

# תוצרי בנייה מקומיים
*.tsbuildinfo
# האייקונים נוצרים מ-`npm run icon` ואינם נשמרים בריפו. ה-workflow של
# השחרור מריץ אותו לפני האריזה – בלי זה electron-builder אורז בלי אייקון.
build/icon.ico
build/icon.png

# תוצרי שחרור
RELEASE_NOTES.md

# ROADMAP – מערכת NEDARIM

מפת דרכים לפיתוח. כל שלב מסתיים ב-**Definition of Done** מפורש; לא עוברים שלב לפני שה-DoD מתקיים ונבדק.
מספרי הדרישות (F-xx, B-xx) מפנים ל-`docs/SPEC.md`.

סטאטוס: `[ ]` לא התחיל · `[~]` בעבודה · `[x]` הושלם

---

## שלב 0 – תשתית פרויקט

- [x] `npm create` פרויקט Electron + Vite + React + TypeScript (electron-vite). מבנה: `src/main` (Electron, DB, שירותים), `src/preload`, `src/renderer` (React).
- [x] ESLint + Prettier + `tsc --noEmit` ב-`npm run check`.
- [x] Vitest לבדיקות יחידה (main). בדיקות רכיבים ל-renderer – נדחה לשלב 2, כשיהיו מסכים אמיתיים.
- [x] `better-sqlite3` + מנגנון מיגרציות (קבצי SQL ממוספרים, טבלת `schema_version`).
- [x] קובץ DB במיקום `app.getPath('userData')/nedarim.db`, WAL, foreign_keys, `integrity_check` בהפעלה.
- [x] שכבת IPC טיפוסית: `src/shared/api.ts` מגדיר חוזה; `preload` חושף `window.api`; ה-renderer לעולם לא ניגש ל-DB ישירות.
- [x] MUI עם `direction: 'rtl'` + stylis-plugin-rtl + גופן Assistant ארוז מקומית (@fontsource, ללא CDN). `<html dir="rtl" lang="he">`.
- [x] `@hebcal/core` בשירות `hebrewCalendar.ts`: תאריך עברי, שנה עברית, פרשת שבוע, שנה כספית.
- [x] `git init`, `.gitignore` (node_modules, dist, *.db, data/backups).
- [x] **כללי-על**: רכיבי היסוד `DataTable` (מיון בכותרת), `FilterBar` (סרגל סינון) ו-`KpiPanel` (סקשן סטטיסטי מתכווץ) – כל מסך בשלבים 2–3 נבנה מעליהם.
- [x] עזר בדיקות: `NEDARIM_SCREENSHOT=<path>` מצלם את החלון ויוצא, לאימות ויזואלי אוטומטי.

**DoD:** ✅ `npm run dev` פותח חלון RTL עם התאריך העברי ופרשת השבוע בכותרת; `npm run check` ו-`npm test` (44 בדיקות) ירוקים; מיגרציה 001 יוצרת את כל הטבלאות וה-Views מ-`docs/DATA-MODEL.md`.

**הערות מימוש:**
- Electron **38** ולא 34: `better-sqlite3@13` דורש Node-API 10, ו-Electron 34 (Node 20) מספק רק 9 – הטעינה קורסת. Electron 38 (Node 22, napi 10) טוען את המודול ללא כל rebuild, כי הוא N-API (ABI יציב). אין צורך ב-`electron-rebuild` ואין צורך ב-Visual Studio Build Tools.
- `better-sqlite3` נטען דרך `createRequire` ולא ב-import סטטי: מנוע ה-ESM של Electron נכשל בפרסור מודול CJS עם binding נייטיב.
- `MuiStack` מוגדר `useFlexGap: true` בתמה – אחרת MUI ו-stylis-plugin-rtl הופכים את ה-margin פעמיים והרווח נופל בצד הלא נכון.

## שלב 1 – סכימה, seed וייבוא מה-Excel

- [x] מיגרציה 001: כל הטבלאות, האינדקסים וה-Views מ-`docs/DATA-MODEL.md`.
- [x] Seed: משתמש admin, אמצעי תשלום, סוגי תרומה, קטגוריות הוצאה, occasions (54 פרשות + 7 מחוברות + חגים + אירועים + זיכויים) עם `hebcal_key`.
- [x] `tools/import/`: סקריפט Node (SheetJS) שקורא את `data/legacy/*.xlsm` לפי `docs/legacy/WORKBOOK-STRUCTURE.md`.
- [x] נורמליזציית תאריכים (SPEC 7.2) עם דגל `needs_review`.
- [x] `occasion-map.json` – מיפוי טקסט חופשי → occasion + note (233 מחרוזות שונות בקובץ).
- [x] דוח חריגים `data/import-report.md`: כל שורה שלא יובאה או יובאה עם דגל.
- [x] בדיקות התאמה אוטומטיות (SPEC 7.3): יתרה לכל חבר מול E3 בבלוק; Σ תשלומים/תרומות/הוצאות לפי חודש מול `מאזן שנתי`; קבלות ללא כפילות.
- [x] הייבוא אידמפוטנטי (הרצה חוזרת מוחקת ומייבאת מחדש בטרנזקציה אחת).
- [x] סימולציית `MaazanSheet` (`legacySim.ts`) שמסבירה כל סטייה במאזן: באג בקובץ הישן מול מאזן שלא חושב מחדש.
- [x] בדיקת רגרסיה מלאה על קובץ האמת (`import.integration.test.ts`).

**DoD:** ✅ `npm run import -- data/legacy/<file>.xlsm` מסיים בהצלחה; **0 סטיות ביתרות של כל 90 החברים**; סה"כ חיובים, תשלומים ויתרה זהים לקובץ לאגורה; 4 סטיות במאזן החודשי – כולן מוסברות ומקורן בקובץ הישן; דוח החריגים ללא שגיאות.

**תוצאות הייבוא (הרצה על `31.08.2026_בית_הכנסת_ברית_שלום.xlsm`):**

| מדד | ערך |
|---|---|
| חברים | 90 |
| יתרות פתיחה | 38 |
| חיובי נדר / זיכויים | 1,201 / 67 |
| תשלומי נדר | 409 |
| תרומות | 43 (מתוך 45 שורות; 2 ריקות בסכום 0) |
| הוצאות | 169 |
| קבלות | 451 |
| סה"כ חיובים · תשלומים · יתרה | 423,661 ₪ · 378,709 ₪ · **44,952 ₪** – זהה לקובץ |
| שגיאות בדוח החריגים | 0 |

**תיקונים למסמכי האיפיון שנבעו מהייבוא:**
- **451 קבלות ולא 452** – מספר 336 הוקצה בגיליון המונה אך אין לו תשלום או תרומה.
- **169 הוצאות ולא 170** – השורה האחרונה בגיליון היא שורת סיכום.
- **`סיכום נדרים` מסונכרן במלואו** מול הכרטיסיות – SPEC ממצא #11 אינו תקף לגרסה זו של הקובץ.
- **אין לעצור בשורה ריקה** בסריקת הכרטיסייה – שתי שורות חיוב מופיעות אחרי רווח.
- **נדרשה תמיכה בסכומים שליליים** בתשלומים ובתרומות (ראו DATA-MODEL, שינוי #8).

## שלב 2 – ליבה: חברים, כרטיסייה, נדרים, תשלומים, קבלות

- [x] מסך חברים (F-10..F-14): טבלה עם מיון וסינון, חיפוש חופשי, הוספה, עריכה, השבתה, אזהרת כפילות שם, מיזוג בשירות.
- [x] כרטיסיית חבר (F-20..F-25): תנועות מ-`v_ledger` עם יתרה מצטברת, תאריך עברי לכל שורה, סינון תקופה (כל התקופות / שנה כספית / טווח מותאם), ללא מגבלת מספר תנועות.
- [x] הזנת נדר (F-30..F-34) + הזנה מרובה (F-33) + ברירת מחדל פרשה מ-hebcal לפי השבת האחרונה.
- [x] זיכוי/תיקון (F-35, F-36) עם סף אישור מנהל שנקרא מההגדרות.
- [x] קבלת תשלום (F-40..F-44) עם "שמור והדפס קבלה" ו"שמור בלי קבלה", אימות חי מול ה-main, כפתור "מלוא היתרה" ו-5 החיובים האחרונים.
- [x] שירות קבלות (F-70..F-74): הקצאת מספר ב-`BEGIN IMMEDIATE`, תבנית HTML לפי נספח ב', PDF דרך `webContents.printToPDF`, שמירה ב-`userData/receipts/YYYY/NNNN.pdf`, הדפסה, סימון "העתק", ביטול עם סיבה, ספר קבלות ובדיקת רציפות.
- [x] סכום במילים בעברית (`amountToHebrewWords`) עם 62 בדיקות יחידה, כולל הבחנת מין דקדוקי בין שקלים לאגורות.
- [x] בדיקות יחידה לשירותים: יתרה, הקצאת מספר קבלה (כולל **בדיקת מקביליות משני חיבורים**), חישוב פרשה, אימותים, הרשאות.
- [x] מסך "ממתינים לקבלה" (F-03) עם הפקה בלחיצה אחת.
- [x] כל הטבלאות: מיון בכותרת, סרגל סינון וסקשן KPI מתכווץ (כללי-על 14–16).

**DoD:** ✅ נבדק על נתוני האמת שיובאו: כרטיסיית חבר 35 (ישראל ישראלי, 121 תנועות, יתרה 12,816 ₪) נפתחת ומציגה יתרה מצטברת נכונה; **קבלה 453 הופקה, נשמרה כ-PDF** (`receipts/2026/0453.pdf`) עם שנה עברית תשפ״ו מחושבת וסכום במילים; `npm run check` ירוק; 285 בדיקות Vitest ירוקות.

**החלטות מימוש:**
- `printToPDF` מקבל את הגופן העברי מוטמע כ-base64 בתוך ה-HTML, כדי שהקבלה תיראה זהה גם במחשב בלי Assistant מותקן.
- `receipt.payer_name` מוקפא בעת ההפקה: שינוי שם החבר אחר כך לא משנה קבלה שכבר הודפסה (F-12, B-04).
- הרשאות כבר עוברות כפרמטר (`userId`, `role`) לכל שירות, למרות שבשלב זה המשתמש תמיד admin – המעבר בשלב 4 יהיה החלפת מקור הנתונים בלבד.
- עזרי בדיקה: `NEDARIM_DB_DIR` (הרצה מול DB אחר), `NEDARIM_SCREENSHOT_HASH` (מסך פתיחה), `NEDARIM_PRINT_RECEIPT` (הפקת PDF ללא ממשק), `NEDARIM_DEV_SCRIPT` (הרצת תרחיש בממשק לפני הצילום – לשחזור תקלות שקורות רק ברצף פעולות). ניתוב לפי hash: `#/card/35`, `#/settings/backup`, `#/receipt/379` (פותח את התצוגה המקדימה של הקבלה לפי מזהה).
- `formatAgorot` מציג אגורות תמיד בשתי ספרות כשהן קיימות ("1,281.60 ₪") – בקבלה, אגורה שנעלמת בתצוגה נראית כטעות.

## שלב 3 – תרומות, הוצאות, מאזן ודוחות

- [x] תרומות (F-50..F-52) כולל קישור לחבר, הפקת קבלה, וחסימת עריכה אחרי קבלה.
- [x] הוצאות (F-60..F-62) כולל צירוף קובץ לארכיון, החזרים, וסיכום דינמי לפי הסינון.
- [x] מסך ראשי / Dashboard (F-01..F-06): כרטיסי סיכום, עשרת החייבים, ממתינים לקבלה, פעולות מהירות, תאריך עברי ופרשה.
- [x] מאזן חודשי/שנתי דינמי (F-80) עם בחירת שנה כספית/עברית/אזרחית/טווח חופשי + דוח צבירה מול גבייה.
- [x] דוחות (F-81..F-86): יתרות חוב, לפי פרשה, תרומות, הוצאות, לפי אמצעי תשלום, דף חשבון לחבר.
- [x] ייצוא Excel / CSV לכל דוח (F-87), עם BOM כדי ש-Excel בעברית יקרא נכון.
- [x] הגדרות ורשימות (F-90..F-92): מסך הגדרות שנבנה מ-spec, מספור קבלות ניתן לקביעה, ניהול רשימות ערכים.

**DoD:** ✅ **המאזן זהה לגיליון `מאזן שנתי` הישן ב-34 מתוך 37 החודשים**, ושלושת החריגים מוסברים ונעולים בבדיקה אוטומטית (`import.integration.test.ts`). כל הדוחות נפתחים ומיוצאים. 355 בדיקות ירוקות; `npm run check` ירוק.

**מה שהמערכת החדשה מראה והקובץ הישן לא יכול:**
- **39 חודשים במקום 37** – 10/2026 ו-12/2026 נספרים עכשיו. המאקרו הישן היה מקודד ל-37 תאים (ממצא #1).
- **דוח צבירה מול גבייה** – כמה נדרו מול כמה נגבה בכל חודש, כלומר פער הגבייה. לא היה קיים כלל.
- **סיכום הוצאות דינמי לפי הסינון**, במקום שורת סיכום ידנית בתחתית הגיליון.

**הגדרות – כלל 12 בפועל:** אין ערך עסקי אחד קשיח בקוד. `SETTING_SPECS` ב-`configuration.ts` מגדיר כל הגדרה עם התווית, הטיפוס והאימות שלה, והמסך נבנה ממנו. מספור הקבלות ניתן לקביעה כלפי מעלה בלבד – בית כנסת שמגיע מפנקס נייר שנגמר ב-1000 מתחיל מ-1001, ומי שכבר הפיק קבלה 500 לא יוכל לרדת אליה (B-03).

## שלב 4 – הקשחה ואריזה

- [x] כניסה בסיסמה, תפקידים (SPEC 6.3), נעילה אוטומטית אחרי חוסר פעילות.
- [x] יומן ביקורת מלא (F-93, B-09, B-10) + מסך צפייה עם סינון, מיון, KPI וייצוא.
- [x] גיבוי (F-100..F-102) ושחזור (F-101) עם גיבוי-לפני-שחזור ואימות SHA-256.
- [x] ייצוא מלא ל-Excel (F-104) – 10 גיליונות, גיליון לכל ישות.
- [x] electron-builder → NSIS installer ל-Windows x64, אייקון שנוצר מקוד, שם מוצר "נדרים".
- [x] עדכון סכימה אוטומטי בהפעלה (מיגרציות) – מיגרציה 003 מוסיפה `require_login` ואינדקסים ליומן.
- [x] מדריך משתמש `docs/USER-GUIDE.md`.

**DoD:** ✅ 406 בדיקות ירוקות, `npm run check` ירוק, ההתקנה נבנית ל-`dist/nedarim-setup-0.1.0.exe`.
**השחזור הוכח מקצה לקצה** (`stage4.test.ts`): גיבוי → שינוי נתונים → שחזור אחרי מחיקת `nedarim.db`, `-wal` ו-`-shm` מהדיסק → סך היתרות, מספר החברים, מספר הקבלות והמונה `sequence.receipt` חוזרים זהים לחלוטין, והרשומה שנוספה אחרי הגיבוי נעלמת.

**החלטות שלב 4 שסוטות מהתכנון המקורי, ולמה:**
- **גיבוב סיסמאות ב-`scrypt` ולא ב-bcrypt** (SPEC 6.3). bcrypt הוא מודול נייטיב שידרוש prebuild נוסף בכל שדרוג Electron; `scrypt` מובנה ב-Node, חזק לא פחות, והפרמטרים נשמרים בתוך ה-hash עצמו כך שאפשר להחמיר אותם בעתיד בלי לשבור סיסמאות קיימות.
- **שם המוצר הוא "נדרים" ולא "נדרים – בית הכנסת דוגמה".** שם בית כנסת בתוך ההתקנה סותר את כלל 12: המערכת מיועדת לשחרור כקוד פתוח, וכל מוריד מקבל התקנה ריקה ומגדיר את שם בית הכנסת שלו במסך ההגדרות.
- **הכניסה בסיסמה כבויה כברירת מחדל** (`require_login = 0`), עם הגדרה להדליק אותה. הגבאי היחיד על מחשב ביתי לא ירצה להקליד סיסמה כדי לרשום נדר; מי שהמחשב שלו משותף מדליק. גם כשההתחברות כבויה כל פעולה נחתמת ב-audit_log בשם משתמש אמיתי, כך שהמעבר בין המצבים לא משנה דבר בשאר המערכת.
- **נעילה אוטומטית: 15 דקות כברירת מחדל, וניתנת לשינוי או לכיבוי (0).** נעילה כפויה על מחשב אישי היא מטרד ולא אבטחה.
- **`npmRebuild: false` באריזה.** better-sqlite3 v13 מגיע עם prebuild של N-API 10, שהוא ABI-יציב וטוב גם ל-Electron 38. בלי זה electron-builder מנסה לקמפל מחדש ודורש Visual Studio ממי שבונה את ההתקנה.
- **הסרת ההתקנה לא מוחקת את תיקיית הנתונים `%APPDATA%/nedarim`.** הנתונים והגיבויים של בית הכנסת שורדים שדרוג והסרה; מחיקה מלאה היא פעולה ידנית ומודעת.

**באג שנתפס בבדיקה החיה של שלב 4:** כל חותמות הזמן במערכת נכתבו ב-`new Date().toISOString()`, כלומר ב-UTC, והוצגו כאילו הן מקומיות – מסך הגיבוי הראה 11:05 לגיבוי שנוצר ב-14:05. חמור מכך: קבלה שהופקה אחרי חצות הייתה מקבלת `issued_at` של היום הקודם, בסתירה ל-`payment_date` שנקבע לפי היום המקומי. תוקן ב-`src/shared/datetime.ts` (`nowIso`/`todayIso`) והוחל על כל 12 הקבצים שכתבו חותמות. נוסף ככלל ברזל 2 ב-CLAUDE.md.

**באג שני שנתפס בבדיקה בעיניים (שלב 4):** השחזור קרא ל-`app.relaunch()`, וההפעלה מחדש של התהליך השאירה מסך לבן לדקות ארוכות – במיוחד בפיתוח, שבו electron-vite מנהל את תהליך Electron. אין לזה שום צורך: אחרי החלפת הקבצים אפשר לפתוח את ה-DB המשוחזר **באותו תהליך** (`closeDatabase` → `applyRestore` → `initDatabase`) ולרענן את ה-renderer. השחזור ירד מדקות ל-**31ms**, ואם הקובץ המשוחזר לא נפתח – גיבוי הבטיחות מוחזר אוטומטית כדי שההתקנה לא תישאר בלי בסיס נתונים.

**תיקיית ארכיון הקבלות ניתנת להגדרה** (`receipts_dir`, מיגרציה 004). הנתיב היה קשיח ב-`userData/receipts` – סתירה לכלל 12. עכשיו אפשר להצביע על תיקיית ענן ולקבל עותק מחוץ למחשב עם כל הפקה. שלוש נקודות שהיו נופלות בשקט בלי טיפול: הגיבוי מגבה את התיקייה **המוגדרת** (אחרת גיבוי של התקנה שהזיזה את התיקייה היה יוצא בלי אף PDF), השחזור מחזיר אליה אחרי שה-DB המשוחזר נפתח (ההגדרה יושבת בתוכו), והנתיב נבדק לכתיבה **בזמן שמירת ההגדרה** ולא ברגע ההפקה מול התורם. קבלות קיימות שומרות את `pdf_path` המלא שלהן ואינן מושפעות.

**מה שהמערכת החדשה נותנת והקובץ הישן לא יכול:**
- **יומן ביקורת שלא ניתן למחיקה מתוך היישום** – בקובץ Excel כל שינוי הוא אנונימי והיסטוריה לא קיימת.
- **גיבוי עם חתימת SHA-256 ומניפסט** – הגבאי רואה לפני השחזור לאיזה מצב הוא חוזר (כמה חברים, כמה קבלות, סך היתרות), וגיבוי פגום פשוט לא ניתן לשחזור.
- **שחזור הפיך** – לפני כל שחזור נוצר אוטומטית גיבוי של המצב הנוכחי.

## שלב 5 – הרצה מקבילה ו-Go-Live

- [ ] 2–4 שבועות עבודה כפולה (קובץ ישן + מערכת) עם השוואת יתרות שבועית.
- [ ] תיקוני UX מהשטח.
- [ ] ייבוא סופי מהקובץ הישן המעודכן, הקפאת הקובץ (Read-only).
- [ ] תיוג `v1.0.0`.

---

## שלב 7 – לוח שנה (F-90)

מסך `לוח שנה` (`CalendarPage`): בחירת טווח תאריכים, ולכל יום – תאריך לועזי,
תאריך עברי מקביל, יום בשבוע, פרשת השבוע, חג אם יש, והאירוע שיוצע בהזנת נדר.

- [x] `calendarRange.ts` – גזירת הטווח. אין טבלת לוח ב-DB; הכול מחושב מהתאריך הלועזי, באותה רוח שבה יתרות מחושבות (כלל 3).
- [x] מיון לפי כותרת, סרגל סינון (טווח, חיפוש, שבתות/חגים) ו-KPI מתכווץ – כללים 14–16.
- [x] ייצוא CSV ו-Excel. `exporters.ts` הורחב ל-`ExportableTable` כדי שטבלה שאינה דוח תקבל ייצוא בחינם, בלי להידחף לתוך האיחוד `ReportId`.
- [x] הדפסה ו-PDF (`calendarPrint.ts` בונה HTML טהור, `htmlPrint.ts` מטפל בחלון) – אותו דפוס כמו הדפסת קבלה.
- [x] 23 בדיקות, כולל מעבר שעון קיץ, גבולות הטווח, בריחה מ-HTML ו-BOM ב-CSV.

**DoD:** `npm run check` ירוק, הרצה חיה מציגה חודש מלא עם פרשות וחגים בעברית,
וייצוא/הדפסה מפיקים את אותו טווח שנראה על המסך. **אומת 12/09/2026.**

---

## שלב 6 – מודול WhatsApp: שליחה המונית (v1.1)

תת-שלבים W0–W5 ו-DoD מפורטים ב-`docs/WHATSAPP-ROADMAP.md`; דרישות ב-`docs/WHATSAPP-SPEC.md`; סכימה ב-`docs/DATA-MODEL.md` (סעיפים "מיגרציה 005" ו-"מיגרציה 007").
תנאי כניסה: DoD של שלב 2 – אומת 2026-09-06. ניתן לפתח במקביל לשלבים 3–4, אך **הפעלה בייצור רק אחרי שלב 5** ועם SIM ייעודי לבית הכנסת.
מיגרציה 005 (הבאה בתור) מוסיפה `member.mobile_e164`/`mobile_status` + backfill חד-פעמי ל-55 המספרים הקיימים, ואת טבלאות המודול.

- [x] W0 – נרמול נייד (מיגרציה 005 + backfill), תבניות, בחירה מרובה
- [~] W1 – חלון WhatsApp Web וסשן (QR) – הקוד הושלם; סריקת QR בטלפון ממתינה לבדיקה ידנית
- [~] W2 – מנוע שליחה להודעה אחת – הקוד הושלם; שליחה אמיתית ממתינה לבדיקה ידנית
- [~] W3 – מנוע קמפיין ומודאל התקדמות – הקוד הושלם ואומת בסימולציה; שליחה אמיתית ממתינה לבדיקה ידנית
- [ ] W4 – היסטוריה, שחזור, הקשחה
- [ ] W5 – בדיקת שטח והפעלה, תיוג `v1.1.0`
- [x] W6 – הודעה בכל אירוע כספי (יומן ההתחשבנות) – מיגרציה 007, W-80..W-89. הזנה מרובה ומצב `auto` נכנסים ל-W3.

---

## שלב 8 – אשף ייבוא נתונים ומחיקת בסיס הנתונים (F-120..F-130)

אשף בן שישה שלבים (שיטת ייבוא · העלאת קובץ · מיפוי שדות · בדיקת תקינות · ייבוא · סיכום)
לכל תשע היישויות שניתן לייבא, בארבעה מצבי פעולה. בהתקנה חדשה הוא נפתח מיד אחרי
אשף ההתקנה – הגבאי שסיים להגדיר עומד מול מערכת ריקה, וזה הרגע שבו הנתונים נכנסים.

- [x] `catalog.ts` – תשע יישויות. **התבנית מדברת בשפת הגבאי ולא בשפת ה-DB**: "מספר חבר 35", לא `member_id`.
- [x] `order.ts` – מיון טופולוגי מ-`dependsOn`. חברים לפני נדרים, אמצעי תשלום לפני תשלומים.
- [x] `template.ts` – תבנית אקסל אחת, גיליון ליישות, שדות חובה באדום ורשות בירוק, שורת דוגמה מסומנת "למחוק".
- [x] `parse.ts` + `validate.ts` – פרסור ואימות טהורים. פותר ההפניות מוזרק, ולכן "תרומה לחבר 47 שאינו קיים" נבדק בלי DB.
- [x] `modes.ts` + `execute.ts` – ארבעה מצבים, טרנזקציה אחת. `replace` חסום על תשלומים ותרומות כשקיימות קבלות (כלל 4).
- [x] `read.ts` – קריאת xlsx ו-CSV (RFC 4180, מרכאות, ירידות שורה, BOM), וזיהוי שורת הכותרות.
- [x] `mapping.ts` – הצעת מיפוי עם רמת ודאות. "שם פרטי" ו"שם משפחה" אינם מתחלפים.
- [x] `detect.ts` – זיהוי היישות לפי שם הגיליון או לפי הכותרות, ורק כשכל שדות החובה מכוסים. זיהוי תיקיית גיבוי והפניה לשחזור.
- [x] `session.ts` – חיבור השלבים, ואימות **בסדר הייבוא** כדי שחבר שבגיליון אחר באותו קובץ ייחשב קיים.
- [x] הרצה יבשה (`dryRun`) – שלב 5 מציג את מה שייכתב מתוך הרצה של הקוד האמיתי שמתגלגלת אחורה, ולא הערכה.
- [x] אשף בן שישה שלבים ב-renderer, ולשונית "ייבוא ונתונים" בהגדרות.
- [x] המשכיות מאשף ההתקנה בהתקנה חדשה.
- [x] F-130 – מחיקת בסיס הנתונים: **גיבוי כפוי** לפני המחיקה, הקלדת שם בית הכנסת לאישור, רישום ביומן לפני המחיקה (כדי שייכנס לגיבוי), ופתיחה מחדש של בסיס נתונים ריק.

**DoD:** `npm run check` ירוק, 1,087 בדיקות עוברות, והרצה חיה עברה את כל שישה השלבים
על קובץ שנבנה כמו קובץ של גבאי (כותרות שונות, עמודה מיותרת, שדה חובה ריק, תרומה לחבר
שאינו קיים): שתי השגיאות המתוכננות דווחו, 3 חברים ו-2 תרומות נכתבו, והמחיקה יצרה גיבוי
עם אותם נתונים ופתחה מערכת ריקה. **אומת 12/09/2026.**

---

## מחוץ לתכולה (שלב ב' עתידי)

- צירוף קבלה כ-PDF להודעת וואטסאפ / שליחת קבלה במייל (שליחה המונית ב-WhatsApp – שלב 6).
- קבלה מרוכזת למספר תשלומים (F-75).
- סנכרון בין שני מחשבים.
- אישור מס הכנסה (סעיף 46) – רק אם לבית הכנסת יש עמותה מוכרת.

## החלטות פתוחות

### נסגרו

- **יתרות פתיחה** → `member.opening_balance_agorot` (ולא שורות `vow_charge`), בגלל יתרת פתיחה שלילית בקובץ. אושר.
- **קבלה 452 → 451**: המונה הבא הוא 453 בכל מקרה, כי `MAX(receipt_number)+1`.
- **תרומה בתאריך 27.07.2925** תוקנה ל-2025 בייבוא, עם רישום בדוח החריגים. אושר.
- **תשלום ללא קבלה** (חבר 16, 816 ₪) יובא ללא קבלה, בסטאטוס "שולם". אושר.
- **מפתחות hebcal של פסח וסוכות** (מיגרציה 008): `occasion.hebcal_key` נקרא רק במסלול `getSedra`, ולכן הוא מחזיק את מפתח הסדרה ולא את מזהה האירוע. `Pesach I`/`Sukkot I` הוחלפו ב-`Pesach`/`Sukkot`, ושתי שבתות חול המועד קיבלו מפתח. אושר 11/09/2026.
- **רישיון: GPL-3.0-or-later.** `@hebcal/core` מופץ תחת GPL עם אופציית "or later" בכותרות המקור, ולכן MIT מעולם לא היה אפשרי. GPLv3 (ולא v2) נבחר כדי ש-`cssjanus` (Apache-2.0, מגיע עם ה-RTL) יהיה תואם. הוחלט 12/09/2026.
- **`xlsx` הוחלף ב-`write-excel-file`** בצד הכתיבה (MIT + fflate, אפס Apache). `xlsx` נשאר ב-devDependencies לכלי הייבוא בלבד ואינו נארז. הוחלט 12/09/2026.
- **חג באמצע שבוע גובר על השבת האחרונה** בברירת המחדל של הזנת נדר (F-31). בשבת הקריאה עדיין מנצחת, כי `getSedra` מדייק יותר בשבת חול המועד. טבלת `occasion` היא המסננת – רק חג עם שורה פעילה ומפתח נבחר. אושר 12/09/2026.
- **שם החג בעברית** מגיע מ-hebcal עצמו (`render('he-x-NoNikud')`) ולא מטבלת מיפוי, כי `getHolidaysOnDate` מחזיר מזהים כמו `Chanukah: 8th Day` שאין להם שורה ב-`occasion`. אושר 11/09/2026.

### לדיון – גיבוי ושחזור לגוגל שיטס

**התבקש 12/09/2026.** גיבוי ושחזור מול Google Sheets, בנוסף לקובץ המקומי.

**התנגשות שדורשת הכרעה לפני מימוש:** זה ענן, רשת ו-OAuth – כלומר סותר את
כלל ברזל 11 ("אין תלות ברשת") ואת מדיניות הפרטיות שמוצגת **בתוך
האפליקציה** ואומרת "אין שירותי ענן, אין סנכרון, אין גיבוי אוטומטי לשרת
כלשהו". אי אפשר לממש בלי לשנות את שתיהן.

בנוסף, גיבוי לגוגל שיטס אינו שווה-ערך לגיבוי הקיים:

| | גיבוי מקומי | גוגל שיטס |
|---|---|---|
| נאמנות | עותק בינארי מדויק + SHA-256 | טבלאות שטוחות; מבנה, טיפוסים ו-FK אובדים |
| שחזור | אטומי, מובטח | הרכבה מחדש – עלול להיכשל חלקית |
| פרטיות | נשאר במחשב | 90 חברים עם טלפונים וחובות אצל גוגל |

**הצעה:** לא "גיבוי" אלא **ייצוא לגוגל שיטס** – נוח לצפייה ולשיתוף עם ועד
בית הכנסת, ובמפורש לא תחליף לגיבוי. השחזור יישאר מהקובץ המקומי בלבד.
דורש אישור לפני מימוש.

### לאישור הגבאי לפני שלב 2

1. **קבלה 336** – המספר קיים בגיליון המונה אך אין לו תשלום או תרומה. קבלה שהודפסה ובוטלה, או תקלה באקסל?
2. **"סיכום שנת תשפ"ג"** רשום בקובץ כחבר מס' 61 עם חיוב ותשלום זהים (123,617 ₪ כל אחד, יתרה 0). יובא כחבר **לא-פעיל**. למחוק אותו מהמערכת החדשה, או להשאיר לצורך התאמה היסטורית?
3. **65 רשומות מסומנות `needs_review`** – בעיקר פרשות לא מזוהות ואמצעי תשלום פגומים. לעבור עליהן בדוח ולהחליט אם לתקן לפני שלב 2.

### לאישור לפני שלב 3

4. מאזן על בסיס מזומן (כמו היום) כברירת מחדל – מאושר? (SPEC 5.10)
5. גודל נייר לקבלה: A5 או A4?
6. האם להדפיס תאריך עברי על הקבלה?
7. סף אישור מנהל לזיכוי: 500 ₪ (כרגע מוגדר 500 ₪ ב-seed).

# תרומה לנדרים

תודה שאתה שוקל לתרום. הפרויקט נכתב עבור גבאי אחד בבית כנסת אחד, והוא
משוחרר בחינם כדי שכל בית כנסת יוכל להשתמש בו.

## לפני שאתה כותב קוד – פתח Issue

**אל תשקיע שעות בקוד שאולי לא יתקבל.** פתח Issue, תאר מה אתה רוצה לעשות,
וחכה לתשובה. זה חוסך זמן לשני הצדדים.

לתקלה: תאר מה עשית, מה ציפית שיקרה, ומה קרה בפועל. צילום מסך עוזר מאוד.

## כללי הברזל של הפרויקט

`CLAUDE.md` מחזיק את הכללים המלאים. אלה שנשברים הכי הרבה:

1. **סכומים באגורות** (`INTEGER`), לעולם לא `float`. המרה לתצוגה רק בממשק.
2. **חותמות זמן בשעון מקומי** דרך `nowIso()` מ-`src/shared/datetime.ts`.
   `new Date().toISOString()` מחזיר UTC, מציג שעה שקרית על הקבלה, ואחרי
   חצות אף נותן תאריך עסקי שגוי.
3. **יתרות מחושבות, לא נשמרות.** להשתמש ב-`v_member_balance`.
4. **מספר קבלה מוקצה רק ב-`BEGIN IMMEDIATE`**, לעולם לא משוחרר ולא נערך.
5. **קבלה היא Immutable.** תיקון = ביטול + הפקה חדשה.
6. **מחיקה לוגית בלבד** (`deleted_at`) + רשומת `audit_log` לכל שינוי כספי.
7. **ה-renderer לא ניגש ל-DB.** הכול דרך `window.api` עם טיפוסים
   מ-`src/shared/api.ts`.
8. **אין ערכים עסקיים קשיחים.** שם בית כנסת, מספר קבלה מתחיל, ספי אישור –
   הכול ב-`setting`. המערכת מיועדת לכל בית כנסת, לא לאחד מסוים.
9. **אין תלות ברשת.** אין טלמטריה, אין ענן, אין עדכון אוטומטי.
10. **מיגרציה שהוחלה היא קפואה.** שינוי = קובץ חדש עם המספר הבא.
11. **כל טקסט ממשק ב-`src/renderer/src/i18n/he.ts`**, לא מפוזר ברכיבים.
12. **RTL בכל מסך חדש.** `textAlign: 'start'`, לא `'right'`.

## איך עובדים

```bash
npm install
npm run dev      # הרצה
npm run check    # טיפוסים + lint – חייב לעבור
npm test         # בדיקות – חייב לעבור
```

**בדיקות אינן אופציונליות.** כל שירות ב-`src/main/services` מגיע עם בדיקות
Vitest. לקוד כספי (יתרה, קבלות, מאזן) נדרש כיסוי לוגי מלא – טעות שם עולה
כסף אמיתי למישהו.

### הודעות commit

בעברית או באנגלית, ובלבד שיהיה ברור **למה** ולא רק מה. אם התיקון נוגע
לדרישה מתועדת – ציין את מספרה (`F-31`, `W-86`, `B-07`).

```
fix(calendar): שבת חול המועד פסח לא קיבלה ברירת מחדל

getSedra מחזיר `Pesach Shabbat Chol ha-Moed`, ובטבלה לא היה מפתח
תואם. נסרקו 12 שנים כדי לוודא שאין מקרים נוספים.
```

## מה קורה אחרי שפתחת Pull Request

1. **GitHub מריץ את הבדיקות אוטומטית.** אם משהו אדום – תקן לפני שממשיכים.
2. **המתחזק קורא ומגיב.** ייתכנו שאלות או בקשות לשינוי. זה חלק נורמלי
   מהתהליך ולא ביקורת עליך.
3. **המיזוג נעשה על ידי המתחזק בלבד.** שום קוד לא נכנס בלי אישור מפורש.

## דברים שסביר שיידחו

- הוספת תלות חדשה בלי דיון. כל חבילה היא רישיון לבדוק ושטח תקיפה נוסף,
  והפרויקט תחת GPL – רישיון לא תואם הוא בעיה אמיתית.
- כל דבר ששולח נתונים החוצה.
- ערך עסקי קשיח בקוד ("בית הכנסת שלי", מספר קבלה מתחיל).
- קוד בלי בדיקות בנתיב כספי.
- שינוי במיגרציה קיימת.

## רישיון

הפרויקט תחת **GPL-3.0-or-later**. בשליחת Pull Request אתה מסכים שהתרומה
שלך תשוחרר תחת אותו רישיון.

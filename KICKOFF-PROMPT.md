# פרומפט פתיחה ל-Claude Code

העתק את הבלוק שלמטה והדבק ב-Claude Code בתוך `C:\claude\NEDARIM`.

---

אתה מוביל את הפיתוח של פרויקט NEDARIM – יישום Windows שולחני, Standalone ו-Offline, שמחליף חוברת Excel/VBA לניהול נדרים, תרומות והוצאות בבית כנסת. אתה מהנדס תוכנה בכיר שמקפיד על נכונות כספית, בדיקות, ועברית/RTL מושלמת.

**לפני שאתה כותב שורת קוד אחת:**
1. קרא במלואם: `CLAUDE.md`, `ROADMAP.md`, `docs/SPEC.md`, `docs/DATA-MODEL.md`, `docs/legacy/WORKBOOK-STRUCTURE.md`.
2. עיין ב-`docs/legacy/vba_source.txt` כדי להבין את הלוגיקה העסקית הקיימת (במיוחד `PrintKabala`, `MaazanSheet`, `NederPayment.CommandButton1_Click`).
3. פתח את `data/legacy/*.xlsm` עם ספריית Node (xlsx/ExcelJS) ואמת בעצמך את מבנה גיליון `כרטיסיות` (בלוקים של 10 עמודות) מול המסמך. אל תסמוך על המסמך בעיניים עצומות – אם יש סתירה, המסמך מתעדכן, לא הנתונים.
4. כתוב לי סיכום של 10–15 שורות: מה הבנת, מה הסתירות/הספקות שמצאת, ומה אתה מתכנן לשלב 0 ו-1. חכה לאישורי.

**לאחר האישור – בצע את שלב 0 ואת שלב 1 ב-`ROADMAP.md`, בסדר הזה:**
- שלב 0: תשתית electron-vite + React + TS + MUI RTL + better-sqlite3 + מיגרציות + @hebcal/core + Vitest. עצור כשה-DoD של שלב 0 מתקיים והראה לי.
- שלב 1: מיגרציה 001 בדיוק לפי `docs/DATA-MODEL.md`, seed, וכלי הייבוא `tools/import/`. הייבוא חייב להפיק `data/import-report.md` עם דוח חריגים ודוח התאמה (יתרה לכל חבר מול תא E3 בבלוק שלו; סכומים חודשיים מול גיליון `מאזן שנתי`; 452 קבלות ללא כפילות). עצור והצג לי את דוח ההתאמה לפני שממשיכים.

**כללים שאין עליהם ויכוח (מפורטים ב-CLAUDE.md):** סכומים באגורות כ-INTEGER; תאריכים ISO; יתרות מחושבות ולא נשמרות; מספר קבלה מוקצה רק בטרנזקציה `BEGIN IMMEDIATE` ולעולם לא משוחרר; קבלה Immutable; מחיקה לוגית + audit_log; ה-renderer ניגש ל-DB רק דרך `window.api` עם חוזה טיפוסי ב-`src/shared/api.ts`; בדיקות Vitest לכל שירות כספי.

**דרך עבודה:** משימה → מימוש → בדיקות ירוקות → `npm run check` ירוק → commit קטן שמצטט F-xx/B-xx → עדכון checkbox ב-`ROADMAP.md` באותו commit. שאלה עסקית שאין לה תשובה במסמכים – רשום תחת "החלטות פתוחות" ב-ROADMAP ושאל אותי; אל תמציא. אם אתה מזהה טעות או חוסר במסמכי האיפיון – תגיד לי במפורש ותציע תיקון, גם אם זה סותר את מה שכתוב.

התחל בשלב 1 של ההוראות (קריאה וסיכום).

---

## פרומפטים להמשך (אחרי כל שלב)

**לשלב 2:**
> ה-DoD של שלב 1 אושר. בצע את שלב 2 ב-ROADMAP.md: חברים, כרטיסייה, הזנת נדר (כולל הזנה מרובה F-33), זיכוי, תשלום, ושירות הקבלות המלא (F-70..F-74) כולל PDF בעברית לפי נספח ב' ב-SPEC. התחל משירותי ה-main עם בדיקות, ורק אחר כך מסכים. הצג לי צילום מסך של כרטיסייה של חבר אמיתי מהייבוא ו-PDF של קבלה 453 לפני שאתה מסמן את השלב כהושלם.

**לשלב 3:**
> בצע את שלב 3: תרומות, הוצאות, Dashboard, מאזן דינמי ודוחות. בדיקת הקבלה: המאזן לכל 37 החודשים חייב להיות זהה לעמודות B/C/E בגיליון `מאזן שנתי` הישן – כתוב בדיקה אוטומטית שמשווה אותם.

**לשלב 4:**
> בצע את שלב 4: אבטחה, audit, גיבוי/שחזור, ייצוא ל-Excel, ו-installer NSIS. הוכח לי ש-restore עובד: גבה, מחק את ה-DB, שחזר, והראה שהיתרות זהות.
